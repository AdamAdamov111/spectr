# SPECTR local stand — copy to .env. Never commit real secrets; on the isolated perimeter secrets come from OpenBao/Vault.
MINIO_ROOT_USER=spectr
MINIO_ROOT_PASSWORD=change-me-change-me
PG_PASSWORD=change-me
OS_ADMIN_PASSWORD=Spectr-Admin-2026!
KC_ADMIN_PASSWORD=change-me
# image tags pinned after the first successful `make up`
SPECTR_IMAGE_TAG=2026.09
